# خطة تطوير ميزات إدارة العقود، كشف الحسابات، والصيانة

## 1. الوضع الحالي

### نموذج إنشاء العقد
- الشاشة الحالية في `lib/features/contracts/contracts_screen.dart` تحتوي على:
  - اختيار العقار
  - اختيار المستأجر
  - قيمة الإيجار
  - التأمين
  - مدة العقد
  - دورية الدفع
  - بنود إضافية
- العقد يُنشأ عبر `ContractsRepository.approveContract(...)`.
- عند إنشاء العقد تُحدث:
  - وحدة العقار (`units`) لتصبح `leased`
  - بيانات المستأجر لتصبح `active`
  - جدول الاستحقاقات (`rent_schedules`) حسب مدة العقد

### نماذج الصيانة
- يوجد شاشة بلاغات الصيانة في `lib/features/settings/maintenance_screen.dart`.
- بلاغ الصيانة يدعم:
  - اختيار العقار
  - اختيار المستأجر
  - وصف المشكلة
  - الأولوية
  - التكلفة
- البيانات محفوظة في جدول `maintenance_tickets`.

### حالة الوحدات والعقود
- يوجد جداول `properties`, `units`, `tenants`, `contracts`, `payments`, `rent_schedules`, `utility_bills`, `maintenance_tickets`.
- بعض السجلات الحالية تستخدم `unitId` في `tenant` أو `contract`، لكن ليس هناك اختيار وحدة مفصل داخل نموذج إنشاء العقد.

## 2. المتطلبات الرئيسية الجديدة

### 2.1 اختيار الوحدة أثناء إنشاء العقد
- عندما يختار المستخدم العقار، يجب عرض قائمة الوحدات الخاصة به بدلاً من الاكتفاء باسم العقار فقط.
- القائمة يجب أن تعرض:
  - الأدوار والشقق
  - الدكاكين/المحلات
  - الحالة الحالية لكل وحدة (متاحة، مؤجرة، صيانة)
- الهدف: يختار المستخدم واحداً من الوحدات المناسبة للعقار قبل حفظ العقد.

### 2.2 كشف حساب لكل مستأجر
- شاشة أو تبويب ضمن تفاصيل المستأجر يوضح:
  - مجموع ما دفعه المستأجر خلال فترة الإيجار
  - مجموع المستحقات
  - الفواتير المفتوحة (الماء، الكهرباء)
  - الأقساط المكتملة، الجزئية، وغير المدفوعة
  - رصيد التأمين المتبقي
- يجب أن يعرض كشف الحساب بترتيب زمني وبتصنيف واضح للحالة.

### 2.3 تحسين عرض حالة الأقساط
- كل قسط في جدول `rent_schedules` يجب أن يكون له حالة واضحة:
  - مدفوع بالكامل
  - مدفوع جزئياً
  - غير مدفوع
  - ملغى / مرفوض إذا كان ذلك مقبولاً
- عرض مرشح يسمح برؤية الأقساط المكتملة فقط، أو المتأخرة فقط، أو المفتوحة.

### 2.4 بلاغات العطل والضرر
- نموذج الصيانة يجب أن يسمح بتسجيل:
  - نوع العطل أو الخلل
  - من المسؤول عن التكلفة (`المستأجر` أو `المالك` أو `غير محدد`)
  - ما إذا كانت التكلفة ستُستقطع من التأمين
  - حالة البلاغ (مفتوح، قيد المعالجة، مغلق)
- يجب ربط البلاغ بوحدة محددة ومستأجر إن وجد.

### 2.5 خروج المستأجر وفحص الوحدة
- عند خروج المستأجر، يجب:
  - مراجعة البلاغات المفتوحة والمتأخرة المرتبطة بوحدته
  - حساب التكلفة الكلية للضرر والصيانة التي يتحملها
  - خصم هذه التكلفة من مبلغ التأمين
  - تحديث حالة العقد والوحدة والمستأجر

## 3. خطة التنفيذ المقترحة

### 3.1 تعديل نموذج إنشاء العقد

#### 3.1.1 واجهة المستخدم
- في `lib/features/contracts/contracts_screen.dart`:
  - بعد اختيار العقار، اجلب قائمة الوحدات عبر `unitsProvider` أو استعلام مخصص.
  - عرض `DropdownButtonFormField<UnitRecord>` أو `ListView` للوحدات المتاحة.
  - عرض الوحدة مع نص مثل:
    - `1-101 شقة` أو `S1 محل`
    - الحالة الحالية للواحدة
  - إذا تم اختيار مستأجر موجود، يمكن عرض الوحدة الافتراضية الخاصة به أو تنشيط اختيار وحدة جديدة.

#### 3.1.2 بيانات العقد
- أضف حقل `unitId` إلى `CreateContractInput` و`Contracts` إذا لم يكن موجوداً.
- تأكد من أن `ContractsRepository.approveContract(...)`:
  - يحدث `units.status` و `units.tenant_id` و `units.tenant_name` باستخدام الوحدة المحددة.
  - يخزن `unitId` ضمن العقد وحقول العقد ذات الصلة.
  - إذا كان المستأجر لديه وحدة سابقة، فالتعامل مع ذلك أو منعه.

#### 3.1.3 تحسينات إضافية
- قد تحتاج إلى إضافة حقل `unitFloor` أو `unitType` (شقة، محل) فقط للعرض، لكن `unitNo` كافٍ.
- يمكن استخدام `formatUnitShort(property.type, unit.unitNo)` لعرض النص بشكل مطابق لبقية التطبيق.

### 3.2 كشف حساب المستأجر

#### 3.2.1 واجهة جديدة
- أضف شاشة جديدة مثل `TenantStatementScreen` أو تبويب داخل `TenantDetails`.
- العرض الرئيسي يجب أن يحتوي على:
  - مجموع المدفوعات (`payments` المرتبطة بالمستأجر)
  - مجموع الفواتير (`utility_bills` المفتوحة / المدفوعة)
  - الأقساط (`rent_schedules`) مع حالة كل قسط
  - الأرصدة الحالية والمتراكمة

#### 3.2.2 استعلامات البيانات
- أنشئ مزود `tenantAccountProvider` أو استعلامات منفصلة:
  - `paymentsRepository.watchByTenant(tenantId)`
  - `rentSchedulesRepository.watchByTenant(tenantId)`
  - `utilityBillsRepository.watchByTenant(tenantId)`
  - `maintenanceRepository.watchByTenant(tenantId)` (اختياري)
- احسب:
  - إجمالي المدفوعات
  - إجمالي المستحقات
  - إجمالي الفواتير المفتوحة
  - الرصيد المتبقي من التأمين

#### 3.2.3 عرض الأقساط
- لكل عنصر قسط ضع:
  - رقم العقد
  - تاريخ الاستحقاق
  - المبلغ
  - المدفوع حتى الآن
  - الحالة (`paid`, `partial`, `due`)
  - رابط الدفع أو رابط لعرض تفصيل القسط

### 3.3 صيانة وبلاغات الأعطال

#### 3.3.1 تطوير نموذج الصيانة الحالي
- في `lib/features/settings/maintenance_screen.dart`:
  - أضف حقل `responsibleParty` بقيم مقترحة: `tenant`, `owner`, `other`.
  - أضف حقل `damageCategory` أو `faultType` مثل `تسريب`, `كهرباء`, `سباكة`.
  - أضف حقل `chargeFromDeposit: bool`.
  - اجعل `unitId` منتقلاً من بيانات الوحدة المرتبطة بالعقار والمستأجر.

#### 3.3.2 التوسيع في البيانات
- جدول `MaintenanceTickets` الحالي يحتاج إلى الأعمدة التالية إن لم توجد:
  - `tenantId` للدلالة على المستأجر إن وجد
  - `unitId` للوحدة المحددة
  - `responsibleParty` أو `damageResponsibility`
  - `damageCategory`
  - `chargeDeposit` (boolean)
  - `settlementStatus` مثل `pending`, `approved`, `deducted`
- يمكن استخدام `maintenanceTickets.cost` لحساب المبلغ.

#### 3.3.3 ربط الصيانة بالقصر
- أضف استعلام ليظهر القضايا المرتبطة بوحدة/مستأجر معيّن في كشف الحساب.
- أضف في واجهة الصيانة فلاتر:
  - حسب المستأجر
  - حسب الوحدة
  - حسب الحالة

### 3.4 خروج المستأجر وفحص الوحدة

#### 3.4.1 واجهة إنهاء العقد
- أضف زر أو إجراء في شاشة تفاصيل العقد/المستأجر: `إنهاء العقد` أو `خروج المستأجر`.
- عند الضغط:
  - جلب جميع البلاغات المفتوحة/المغلقة ذات التكلفة المرتبطة بوحدة المستأجر.
  - حساب التكلفة التي تقع على المستأجر وفقاً لحقل المسؤولية.
  - حساب قيمة التأمين المتبقية بعد الخصم.
  - توليد تقرير مختصر عن الخصومات.

#### 3.4.2 تحديث السجلات
- عند إتمام الخروج:
  - غيّر حالة العقد إلى `terminated` أو `ended`.
  - غيّر حالة الوحدة إلى `vacant` أو `maintenance` إذا كانت بها أضرار.
  - غيّر حالة المستأجر إلى `inactive` أو `former`.
  - حدّث `tenants.securityDeposit` و `Tenants.accumulatedDebt` إذا بقيت تكاليف.
  - إذا كان هناك خصم من التأمين، سجل ذلك في جدول أو تقرير خاص.

### 3.5 تحسين كشف الحسابات والفواتير
- اجعل `utility_bills` قابلة للعرض حسب حالة الفاتورة:
  - `open`، `paid`
- أضف `status` إلى قسائم الصيانة لتكون جزءاً من كشف الحساب.
- أبقِ شاشة الدفع قادرة على إضافة الفواتير المفتوحة سواءً كانت فواتير خدمات أم صيانة.

## 4. التعديلات المقترحة في قاعدة البيانات

### 4.1 جدول `contracts`
- أضف حقل `unitId` إذا لم يكن موجوداً.
- أضف حقل `unitType` أو `unitLabel` إن كان مفيداً للعرض.

### 4.2 جدول `tenants`
- إذا لم يكن موجوداً، ضمن `unitId` بالفعل؛ وهذا جيد.
- أضف حقل `contractStatus` أو `tenantStatus` إذا لم يكن واضحاً.

### 4.3 جدول `maintenance_tickets`
- أضف الأعمدة التالية:
  - `tenantId` (nullable)
  - `unitId`
  - `responsibleParty`
  - `damageCategory`
  - `chargeDeposit`
  - `settlementStatus`

### 4.4 جدول `payments` أو `rent_schedules`
- أضف حالة `partial` للقسط إن لم تكن موجودة بالفعل.
- احتفظ بـ `dueScheduleId` و `utilityBillIds` لربط المدفوعات بالأقساط والفواتير.

### 4.5 جدول `utility_bills`
- تأكد من أن `tenantId`, `unitId` معروضان.
- يفضل إضافة حقل `billType` و`periodLabel` لسهولة العرض.

## 5. ترتيب التنفيذ المقترح

1. تعديل نموذج إنشاء العقد لإظهار اختيار الوحدة بعد اختيار العقار.
2. إضافة `unitId` إلى عقود الإيجار وتحديث `ContractsRepository.approveContract`.
3. تطوير شاشة كشف حساب المستأجر (دفعات، أقساط، فواتير، صيانة).
4. تحسين جدول `rent_schedules` لعرض حالات الأقساط بدقة.
5. تطوير شاشة الصيانة لربط مساحة الوحدة والمستأجر، وتحديد المسؤولية عن التكلفة.
6. إضافة إجراءات إنهاء عقد/خروج مستأجر مع خصم من التأمين.
7. اختبار كامل على تسجيل عقد جديد، دفع قسط، إضافة فاتورة، فتح بلاغ صيانة، وإنهاء عقد.

## 6. اقتراحات إضافية قيمة

- إضافة شاشة `Tenant Profile` تشمل:
  - معلومات المستأجر
  - الوحدة الحالية
  - كشف الحساب التفصيلي
  - سجل الشكاوى والصيانة
- إنشاء تقرير فاتورة/كشف شهري قابل للطباعة أو المشاركة عبر WhatsApp.
- إضافة إشعارات أو تذكير تلقائي للإيجارات المتأخرة والفواتير غير المدفوعة.
- دعم ملاحظات نهاية العقد، مثل `inspection notes` و`deduction summary`.
- تمكين فلترة الوحدة حسب النوع (`شقة`, `محل`, `دكان`) في كل شاشة إدارة الوحدات.
- إضافة حقل `reasonForTermination` عند خروج المستأجر.

## 7. ملاحظات مهمة

- التطبيق مخصص للمالك/المشرف فقط، لذلك يمكن تنفيذ كل هذه الميزات في نفس الواجهة دون حاجز تسجيل دخول للمستأجر.
- التركيز يجب أن يكون على:
  - وضوح اختيار الوحدة
  - ربط كل دفعة وفاتورة وصيانة بالوحدة والمستأجر
  - كشف حساب واحد موثوق لكل مستأجر

---

هذه الخطة تشرح كيف نبدأ بالتغييرات الفنية خطوة بخطوة، وكيف ننتقل من نموذج إنشاء عقد بسيط إلى إدارة شاملة للوحدة، الحسابات، والصيانة. إذا تريد، أستطيع الآن أبدأ بتنفيذ المرحلة الأولى: اختيار الوحدة ضمن نموذج العقد. 