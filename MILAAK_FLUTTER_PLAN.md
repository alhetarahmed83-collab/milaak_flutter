# خطة مشروع Flutter موبايل محلي: **مِلاك | Milaak**

## 1. الملخص
- تطبيق جوال محلي جديد بالكامل باسم **Milaak** لإدارة العقارات والمستأجرين والعقود والتحصيل.
- المشروع مستقل عن `srh` ولا يعدل أي ملف داخله.
- لا يوجد تسجيل دخول، ولا Firebase، ولا اعتماد على الإنترنت في الوظائف الأساسية.
- قاعدة البيانات محلية على الجهاز باستخدام SQLite مع Drift.
- التصميم عربي RTL، حديث، متناسق، موبايل أولاً، ومبني على Material 3.

## 2. التقنيات المعتمدة
- Flutter + Dart.
- Material 3.
- go_router.
- Riverpod.
- Drift + SQLite.
- flutter_secure_storage.
- pdf + printing.
- share_plus + file_picker + path_provider.
- flutter_local_notifications.
- fl_chart.
- intl + flutter_localizations.
- local_auth.

## 3. الميزات والشاشات
- لوحة التحكم.
- العقارات.
- المستأجرون.
- العقود.
- التحصيل المالي.
- التقارير.
- التنبيهات.
- النسخ الاحتياطي.
- الإعدادات.

## 4. الهيكل المعماري
- `lib/app`: إعداد التطبيق، الثيم، الراوتر، والتبويبات الرئيسية.
- `lib/core`: الثوابت، الألوان، أدوات التاريخ والعملة.
- `lib/data/local`: قاعدة Drift والجداول.
- `lib/data/repositories`: واجهات التعامل مع البيانات.
- `lib/features`: شاشات ومزايا التطبيق.
- `lib/shared/widgets`: مكونات واجهة مشتركة.
- `lib/services`: PDF، التنبيهات، النسخ الاحتياطي، والمشاركة.

## 5. قاعدة البيانات والتدفقات
- الجداول: `properties`, `tenants`, `contracts`, `payments`, `settings`, `audit_logs`.
- اعتماد العقد يتم داخل transaction واحدة.
- تسجيل سند قبض مدفوع يتم داخل transaction واحدة.
- التقارير تعتمد على بيانات SQLite المحلية.

## 6. تجربة الاستخدام والتصميم
- واجهة عربية RTL بالكامل.
- تبويبات سفلية ثابتة: لوحة، عقارات، مستأجرون، عقود، تحصيل.
- شاشة المزيد تشمل التقارير، الإعدادات، النسخ الاحتياطي، والقفل المحلي.
- أول فتح يعرض إعداداً قصيراً لاسم المكتب فقط.

## 7. خطة الاختبار والقبول
- فتح التطبيق ينشئ قاعدة البيانات تلقائياً.
- إضافة عقار ومستأجر وعقد وسند قبض تعمل من البداية للنهاية.
- البيانات تبقى محفوظة بعد إغلاق التطبيق.
- العقود والسندات تصدر PDF.
- النسخ الاحتياطي والتقارير تعمل محلياً.
- الواجهة لا تتداخل على الشاشات الصغيرة.

## 8. افتراضات
- الأولوية Android مع دعم iOS.
- الذكاء الاصطناعي اختياري لاحقاً.
- لا توجد مزامنة سحابية في الإصدار الأول.
