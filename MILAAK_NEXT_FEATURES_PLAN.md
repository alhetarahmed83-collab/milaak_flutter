# ملف الخطة المقترح: `MILAAK_NEXT_FEATURES_PLAN.md`

## Summary
- إنشاء ملف خطة جديد داخل `C:\Users\HP\Desktop\milaak_flutter` باسم `MILAAK_NEXT_FEATURES_PLAN.md`.
- الهدف: تحديد المرحلة القادمة لتطبيق **مِلاك**: ميزات ناقصة، رسائل للمستأجرين، إعادة تصميم كاملة تناسب مجال الإيجارات، وتحسينات ضرورية للتشغيل اليومي.
- التطبيق يبقى محلياً بدون تسجيل دخول وبدون اعتماد أساسي على الإنترنت.
- الافتراض الافتراضي للرسائل: استخدام SMS/WhatsApp عبر تطبيقات الجهاز، وليس بوابة رسائل مدفوعة في الإصدار القادم.

## 1. إعادة تصميم التطبيق بالكامل
- اعتماد هوية بصرية جديدة أكثر عقارية:
  - ألوان: كحلي عقاري، أخضر تحصيل، ذهبي للعقود، أحمر للمتأخرات، خلفيات رمادية فاتحة.
  - الواجهة تكون أقل “بطاقات عامة” وأكثر “نظام إدارة إيجارات”.
  - استخدام لغة واضحة: مستحق، مدفوع، متأخر، شاغر، مؤجر، عقد منتهي.
- إعادة بناء الصفحة الرئيسية كـ “مركز عمليات”:
  - ملخص اليوم: المستحقات، العقود القريبة، الدفعات الأخيرة، الوحدات الشاغرة.
  - بطاقة صحة المحفظة: نسبة الإشغال، التحصيل، المتأخرات.
  - إجراءات سريعة: سند قبض، إضافة مستأجر، إنشاء عقد، إرسال تذكير.
- تعديل التبويبات السفلية:
  - الرئيسية
  - العقارات
  - المستأجرون
  - المالية
  - المزيد
- شاشة التفاصيل لكل عقار ومستأجر بدل الاعتماد على قائمة فقط.

## 2. نظام الرسائل والتواصل
- إضافة `MessageService` موحد يدعم:
  - SMS عبر تطبيق الرسائل في الجهاز.
  - WhatsApp عبر `wa.me`.
  - مشاركة نص الرسالة عبر النظام.
- إضافة قوالب رسائل جاهزة قابلة للتعديل:
  - رسالة تأكيد سداد.
  - رسالة تذكير قبل موعد الإيجار.
  - رسالة تأخير سداد.
  - رسالة انتهاء عقد قريب.
  - رسالة تجديد عقد.
  - رسالة سند قبض مرفق معها رقم السند والمبلغ.
- استخدام متغيرات داخل الرسائل:
  - `{tenantName}`
  - `{amount}`
  - `{propertyName}`
  - `{unitId}`
  - `{receiptNo}`
  - `{dueDate}`
  - `{officeName}`
- عند تسجيل سند قبض مدفوع:
  - يظهر خيار: “إرسال تأكيد للمستأجر”.
  - الرسالة الافتراضية: تم استلام مبلغ كذا، رقم السند كذا، شكراً لكم.
- عند وجود متأخرات:
  - زر إرسال تذكير من بطاقة المستأجر.
  - زر إرسال تذكير جماعي للمستأجرين المتأخرين.
- إضافة جدول جديد `message_logs` لحفظ:
  - المستأجر
  - نوع الرسالة
  - القناة: SMS / WhatsApp / Share
  - نص الرسالة
  - وقت الإرسال
  - الحالة: opened / failed / cancelled إن أمكن.

## 3. ميزات ناقصة وضرورية
- إدارة الوحدات داخل كل عقار:
  - جدول `units`.
  - رقم الوحدة، الدور، نوع الوحدة، حالة الوحدة، الإيجار المتوقع.
  - الحالات: شاغرة، مؤجرة، صيانة، محجوزة.
- جدول الاستحقاقات:
  - جدول `rent_schedules`.
  - توليد دفعات مستحقة حسب العقد: شهري، ربع سنوي، نصف سنوي، سنوي.
  - عرض المستحق القادم لكل مستأجر.
- المصروفات:
  - جدول `expenses`.
  - مصروف صيانة، كهرباء مشتركة، إدارة، حراسة، أخرى.
  - ربط المصروف بعقار اختياري.
  - التقارير تعرض صافي الدخل = التحصيل - المصروفات.
- المرفقات:
  - جدول `attachments`.
  - حفظ مسار صورة الهوية، صورة العقد، إيصال، صورة الوحدة.
  - كل مرفق مرتبط بمستأجر أو عقد أو سند.
- الصيانة:
  - جدول `maintenance_tickets`.
  - بلاغ صيانة، حالة البلاغ، الأولوية، التكلفة، العقار/الوحدة.
- سجل النشاط:
  - توسيع `audit_logs` ليعرض في واجهة “السجل”.
  - تسجيل اعتماد عقد، حذف عقار، تسجيل سند، إرسال رسالة.

## 4. تحسينات العقود والمالية
- إنشاء عقد لا يكتفي بنص عام:
  - قالب عقد عربي منظم.
  - بنود ثابتة وقابلة للتعديل.
  - بيانات المؤجر والمستأجر والعقار والوحدة.
  - شروط السداد والتأمين والصيانة.
- شاشة سند قبض احترافية:
  - معاينة قبل الحفظ.
  - رقم سند بتسلسل واضح: `REC-2026-0001`.
  - خيار إرسال السند مباشرة بعد الحفظ.
- لوحة مالية:
  - إجمالي المدفوع.
  - إجمالي المتأخر.
  - إجمالي المصروفات.
  - صافي الدخل.
  - أعلى المستأجرين مديونية.
- التقارير:
  - تقرير شهري PDF.
  - تقرير عقار واحد PDF.
  - تقرير مستأجر واحد PDF.
  - تقرير متأخرات PDF.
  - تقرير إشغال الوحدات.

## 5. تغييرات قاعدة البيانات
- رفع `schemaVersion` في Drift من 1 إلى 2.
- إضافة الجداول:
  - `units`
  - `rent_schedules`
  - `expenses`
  - `attachments`
  - `maintenance_tickets`
  - `message_templates`
  - `message_logs`
- تعديل الجداول الحالية:
  - `tenants`: إضافة رقم هوية اختياري، عنوان، ملاحظات.
  - `contracts`: إضافة رقم عقد، شروط مخصصة، حالة التجديد.
  - `payments`: إضافة `dueScheduleId` اختياري، وحالة إرسال الرسالة.
  - `settings`: إضافة العملة، رقم المكتب، قالب الرسائل الافتراضي.
- الترحيل يجب أن يحافظ على البيانات الحالية بدون حذف.

## 6. تجربة الاستخدام الجديدة
- كل كيان مهم له صفحة تفاصيل:
  - تفاصيل العقار: الوحدات، المستأجرون، الدخل، المصروفات، العقود.
  - تفاصيل المستأجر: العقد، السندات، المديونية، الرسائل، المرفقات.
  - تفاصيل العقد: البنود، PDF، إرسال، تجديد، إنهاء.
- إضافة بحث عام أعلى التطبيق:
  - يبحث في العقارات، المستأجرين، الوحدات، العقود، السندات.
- إضافة حالات فارغة ذكية:
  - “أضف أول وحدة”
  - “لا توجد مستحقات هذا الشهر”
  - “كل المستأجرين ملتزمون بالسداد”
- جعل الأزرار الحساسة واضحة:
  - تسجيل سند قبض
  - إرسال تذكير
  - إنشاء عقد
  - تصدير PDF

## 7. خطة الاختبار
- اختبار الرسائل:
  - عند سند مدفوع تظهر رسالة تأكيد جاهزة.
  - زر WhatsApp يفتح المحادثة بالنص الصحيح.
  - زر SMS يفتح تطبيق الرسائل بالنص الصحيح.
  - يتم حفظ سجل الرسالة في `message_logs`.
- اختبار الجداول الجديدة:
  - إنشاء عقار ثم وحدات.
  - ربط مستأجر بوحدة.
  - إنشاء عقد يولد جدول استحقاقات.
  - تسجيل سند يخفض الاستحقاق أو المديونية.
- اختبار التقارير:
  - تقرير مستأجر PDF.
  - تقرير عقار PDF.
  - تقرير متأخرات PDF.
  - تقرير مالي شهري PDF.
- اختبار التصميم:
  - لا يوجد تداخل نصوص في الشاشات الصغيرة.
  - كل الأزرار قابلة للمس بسهولة.
  - RTL صحيح في القوائم والنماذج والـ PDF.
- اختبار الترحيل:
  - بيانات النسخة الحالية تبقى موجودة بعد تحديث قاعدة البيانات.

## 8. Assumptions
- لا يتم إضافة تسجيل دخول.
- لا يتم استخدام بوابة SMS مدفوعة في هذه المرحلة.
- الرسائل تعتمد على تطبيقات الجهاز: SMS وWhatsApp والمشاركة.
- التطبيق يستمر محلياً بالكامل.
- الأولوية: Android أولاً، مع إبقاء iOS متوافقاً قدر الإمكان.
- ملف الخطة المطلوب إنشاؤه عند التنفيذ: `C:\Users\HP\Desktop\milaak_flutter\MILAAK_NEXT_FEATURES_PLAN.md`.
